pragma solidity >= 0.7.0 < 0.9.0;

/*
Boolean/bool = true/false or 1/0
Interger/uint - Non-fractional number
String/string = Letters/Decimal Numbers
*/

contract learnVariables {

    // all soliodity code goes within a contract

    uint chocolateBar = 10;
    uint storeOwner = 300;
    bool lieDetector = true;
    string errorMessage = "Error!";


}

contract variablesExercise {
    
    uint wallet = 500;
    bool spend = false;
    string notifySpend = "You have spent money!";

}